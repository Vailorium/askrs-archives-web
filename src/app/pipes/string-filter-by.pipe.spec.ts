import { StringFilterByPipe } from './string-filter-by.pipe';

describe('StringFilterByPipe', () => {
  it('create an instance', () => {
    const pipe = new StringFilterByPipe();
    expect(pipe).toBeTruthy();
  });
});
