import { HeroSkillFilterByPipe } from './hero-skill-filter-by.pipe';

describe('HeroSkillFilterByPipe', () => {
  it('create an instance', () => {
    const pipe = new HeroSkillFilterByPipe();
    expect(pipe).toBeTruthy();
  });
});
